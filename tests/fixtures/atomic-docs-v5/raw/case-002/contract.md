# Synthetic Program Mapping Contract

Contract ID: `CONTRACT-V5-002`

Authority revision: `REV-V5-002`

Applicability: verified for `CONSUMER-V5-002` and the `schedule_payload` function in `source.py`.

The outbound `programId` field must contain the canonical `programId` value. The human-facing `displayId` is not accepted for this consumer.
