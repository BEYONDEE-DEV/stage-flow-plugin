# Synthetic Contact Projection Contract

Contract ID: `CONTRACT-V5-001`

Authority revision: `REV-V5-001`

Applicability: the `contact_summary` function in `source.py`.

`PRINCIPAL-V5-001` may project a contact summary only from the object whose identifier equals both the authorized and requested object identifier. The object used for projection must have that same identifier even when fields individually are allowed.
