interface:
  display_name: "Atomic Impl"
  short_description: "Implement and verify from approved atomic docs."
  default_prompt: "Use $atomic-impl to document these requirements, implement from the approved atomic docs, and verify changed required AIDs against the final code."
