interface:
  display_name: "Stageflow"
  short_description: "Four-stage goal, review, and approval workflow."
  default_prompt: "Use $stageflow to run this request through definition, implementation plan, and implementation stages."
policy:
  allow_implicit_invocation: true
