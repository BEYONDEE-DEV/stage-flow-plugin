interface:
  display_name: "Simple Workflow"
  short_description: "Plan with Astra and implement approved changes with Sol."
  default_prompt: "Use $simple-workflow to plan and coordinate with Astra, independently challenge plan.md once, and delegate approved write work to Sol."
policy:
  allow_implicit_invocation: true
