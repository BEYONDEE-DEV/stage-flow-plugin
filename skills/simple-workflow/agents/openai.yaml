interface:
  display_name: "Simple Workflow"
  short_description: "Challenge intent before confirming and executing a plan."
  default_prompt: "Use $simple-workflow to challenge inferred intent before confirmation, write plan.md, review it, and prepare goal execution."
policy:
  allow_implicit_invocation: true
