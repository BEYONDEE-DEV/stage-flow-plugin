interface:
  display_name: "Atomic Docs"
  short_description: "Source-guided implementation context for development decisions."
  default_prompt: "Use $atomic-docs to select durable implementation context before writing: classify candidates as write, merge, or drop, keep evidence locator-focused, and preserve only high-value owners, rules, contracts, constraints, approved changes, and unresolved decisions without mirroring source mechanics."
