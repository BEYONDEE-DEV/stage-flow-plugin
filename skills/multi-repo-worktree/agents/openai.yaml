interface:
  display_name: "Multi Repo Worktree"
  short_description: "Multi-repo worktree coordination."
  default_prompt: "Use $multi-repo-worktree to inspect and coordinate this multi-repo worktree bundle."

policy:
  allow_implicit_invocation: false
