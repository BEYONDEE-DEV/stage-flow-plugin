interface:
  display_name: "Multi Repo Worktree"
  short_description: "Permanent worktrees with squash-safe generation branches."
  default_prompt: "Use $stageflow:multi-repo-worktree submit to publish ready PRs while preserving local follow-up work."

policy:
  allow_implicit_invocation: false
